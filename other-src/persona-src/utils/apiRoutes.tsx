const BASE_URL = "/api";
const AUTH = "auth";
const SIGNUP = "register";
const PROFILE = "profile";

export const USER_SIGNUP = `${BASE_URL}/${AUTH}/${SIGNUP}`;
export const CREATE_PROFILE = `${BASE_URL}/${PROFILE}`;
